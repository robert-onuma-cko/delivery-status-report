# secrets
.env

# local data snapshots (reports/ is tracked on purpose)
data/

# static-site bundle for manual uploads; index.html/archive.html are tracked, the zip is rebuilt every run
site.zip

# python
__pycache__/
*.pyc

# legacy Zapier script that still contains hard-coded credentials - keep it local only
weekly_portfolio_report.py
